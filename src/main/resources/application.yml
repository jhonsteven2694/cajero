spring:
  application:
    name: database-app
  server:
    port: 9090
  dbcajero:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/CAJERO?autoreconnect=true"
    password: ""
    username: "root"
    textOnBorrow: true
    testWhiledle: true
    timeBetweenEvictionRunsMillis: 60000
    minEvictableldleTimeMillis: 30000
    validationQuery: SELECT 1
    max-active: 15
    max-idle: 10
    max-wait: 8000  
  
    
